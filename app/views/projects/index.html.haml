.row
  .col-md-4.col-md-offset-4
    %h1 Select a project

    - current_user.projects.each do |project|
      = button_to(project.name, dashboard_path, method: :post, params: {project_id: project.id}, class: "btn btn-default btn-lg btn-block")