.row
  .col-md-4.col-md-offset-4
    %h1 Select a tasklist

    - current_user.tasklists.each do |tasklist|
      = button_to(tasklist.name, dashboard_path, method: :post, params: {project_id: tasklist.project_id, tasklist_id: tasklist.id}, class: "btn btn-default btn-lg btn-block")